<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Permisos requeridos para la aplicaciÃ³n -->

    <!-- Permite la conexiÃ³n a internet para la API y los mapas -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- CORRECCIÃ“N: Se aÃ±aden los permisos para que el mapa funcione -->
    <!-- Permite que la app acceda a la ubicaciÃ³n precisa del GPS -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- Permite que la app acceda a la ubicaciÃ³n aproximada (redes mÃ³viles/Wi-Fi) -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
</manifest>
